---
layout: base
---

<div class="container">
    <h2>{{ title }}</h2>
    <p class="date">{{ published | dateForEpisode }} / {{ duration }}</p>

    <audio controls preload="none">
        <source src="{{ audioUrl }}" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>

    {{ content | safe }}

</div>
